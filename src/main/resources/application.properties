spring.application.name=${SPRING_APP_NAME:springsecurity}

logging.pattern.console=${LOGPATTERN_CONSOLE:%green(%d{HH:mm:ss.SSS}) %blue(%-5level) %red([%thread]) %yellow(%logger{15}) - %msg%n}
logging.level.org.springframework.security=${SPRING_SECURITY_LOG_LEVEL:TRACE}

spring.security.user.name=${SECURITY_USERNAME:zwaks}
spring.security.user.password=${SECURITY_PASSWORD:password}

spring.datasource.url=jdbc:mysql://${DATABASE_HOST:localhost}:${DATABASE_PORT:3306}/${DATABASE_NAME:eazybank}
spring.datasource.username=${DATABASE_USERNAME:root}
spring.datasource.password=${DATABASE_PASSWORD:Sibo@1712}
spring.jpa.show-sql=${JPA_SHOW_SQL:true}
spring.jpa.properties.hibernate.format_sql=${HIBERNATE_FORMAT_SQL:true}

server.servlet.session.timeout=${SESSION_TIMEOUT:20m}

spring.security.oauth2.resourceserver.opaquetoken.introspection-uri=${INTROSPECTION_URI:http://localhost:8180/realms/eazybank-dev/protocol/openid-connect/token/introspect}
spring.security.oauth2.resourceserver.opaquetoken.introspection-client-id=${INTROSPECTION_ID:eazybankintrospect}
spring.security.oauth2.resourceserver.opaquetoken.introspection-client-secret=${INTROSPECTION_SECRET:ZBJKtGRuoPKtA21OB2iWuRAo7ggBJfiE}
